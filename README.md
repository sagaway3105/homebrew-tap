# homebrew-tap

[ClaudeBar](https://github.com/sagaway3105/claude-bar) のHomebrewタップです。

```sh
brew install --cask sagaway3105/tap/claudebar
```

アプリ内のSparkle自動アップデートに対応しているため、インストール後の更新はアプリ自身が行います（`auto_updates true`）。
